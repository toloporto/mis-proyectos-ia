# .github/workflows/quick-tests.yml
# Tests rápidos para cada push

name: Quick Tests

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ main ]

jobs:
  quick-test:
    name: ⚡ Tests Rápidos
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Instalar dependencias
        run: |
          pip install -r requirements.txt
          pip install pytest
      
      - name: Ejecutar tests rápidos
        run: |
          pytest tests/ -v --maxfail=1
